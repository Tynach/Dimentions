-- List of possible privileges
CREATE IF NOT EXISTS TABLE privs (
	priv_id INT UNSIGNED NOT NULL AUTO_INCREMENT,
	priv_name VARCHAR(20) NOT NULL,

	PRIMARY KEY (priv_id),
	UNIQUE INDEX (priv_name)
) ENGINE = InnoDB;